---

## 执行流程说明

### 流程-6：任务执行标准流程

**模块**：开发规范
**状态**：已定义

**描述**

定义 Claude Code Manager 项目的标准任务执行流程，供后续所有功能任务参考。

**执行步骤**

1. 创建分支 feat/xxx（基于 master）
2. 创建 git worktree 独立工作区（.worktrees/feat/xxx）
3. 在 worktree 中执行 tasks.md 的原子任务（每步改一个文件）
4. 执行完成后验证验收标准
5. 验收通过 → 在 worktree 中提交代码（git commit）
6. 合并到主分支（master）并清理 worktree
7. 提交代码到远程（git push）

**注意事项**

- 每次执行前确保 master 分支是最新的
- worktree 路径位于项目根目录的 .worktrees/ 下
- 合并后及时清理 worktree，避免资源占用
- 功能完成后及时推送到远程仓库

---

## 今日任务总结（2026-02-27）

### 已完成任务

#### M2-T06: 进程管理器
- 添加 `ProcessError` 异常类（错误码 50003）
- 创建 `ProcessManager` 类，支持启动/停止任务进程
- 创建进程管理 API 路由（`/api/tasks/{task_id}/start`、`/api/tasks/{task_id}/stop`）
- 在 `main.py` 中注册进程路由

#### M2-T07: 输出解析器与执行引擎整合
- 创建 `OutputParser` 类，解析 Claude Code 输出中的 SUCCESS/FAILURE 标记
- 创建 `ExecutionService` 类，整合 Git 服务、进程管理、Prompt 构建
- 更新 `TaskService` 集成执行引擎，添加 `execute_task` 和 `get_task_by_id` 方法
- 创建执行 API 路由（`/api/tasks/{taskId}/execute`、`/api/tasks/{taskId}/cancel`、`/api/tasks/{taskId}/reset`）
- 在 `main.py` 中注册执行路由

#### M2-T08: 日志服务模块
- 创建 `LogService` 类，支持追加、获取、清空日志
- 添加 `read_json_lines` 工具函数，读取 JSONL 文件
- 创建日志 API 路由（`/api/tasks/{taskId}/logs`、`/api/tasks/{taskId}/logs/stream`）
- 支持 SSE 实时推送日志
- 在 `main.py` 中注册日志路由

### 下一个待完成任务

#### M2-T09: 文件写入模块
- 向被管理项目的文档文件追加经验总结
- 生成 Bug 报告文件
- 读取经验总结全文

---
